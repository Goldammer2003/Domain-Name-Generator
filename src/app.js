/* eslint-disable */

//write your code here

let pronoun = ["the", "our"];
let adj = ["great", "big"];
let noun = ["jogger", "racoon"];
let ending = [".com", ".de", "us"];
for (let i = 0; i < pronoun.length; i++) {
  for (let a = 0; a < adj.length; a++) {
    for (let b = 0; c < noun.length; c++) {
      for (let b = 0; c < noun.length; c++) {
        console.log(pronoun[i] + adj[a] + noun[b] + ending[c]);
      }
    }
  }
}
